<ul id="chat">
  <% @messages.each do |msg| %>
  <li>
  	<%= msg.content %>
  </li>
  <% end %>
</ul>

<%= form_for Message.new, :remote => true do |f| %>
  <%= f.text_field :content %>
  <%= f.submit "Send" %>
<% end %>